package(default_visibility = ["//visibility:public"])

load("@gs_tools//bazel/ts:defs.bzl", "ts_library")


ts_library(
    name = "main",
    srcs = glob([
        "core/**/*.d.ts",
        exclude = [
            "
        ]
    ]),
)
